Place raw and processed project datasets here.

- `raw/` stores downloaded or exported source files.
- `processed/` stores cleaned feature-ready CSVs.

The code also supports your current workspace layout where the Bangalore CSV
already exists at the repository root and the Delhi files already exist in the
top-level dataset folders.
